import { NextRequest, NextResponse } from "next/server";import { getServerSession } from "next-auth";import { prisma } from "@/lib/prisma";import { getSchedule, getMemberMemberships, getMemberMembershipsById, EvoSchedule, EvoMemberMembership, statusContrato, temPagamentoNoMes, tipoDePlano } from "@/lib/evo/queries";import { getTurmaEnrollments, EvoEnrollment, getMemberFixedSchedules, EvoFixedSchedule } from "@/lib/evo/enrollments";import { calcularDiaDaSemana, contribuicaoFixa, CONTRIBUICAO_FREE, round2, contarAulasMes, DIAS_SEMANA, AlunaCalculo, ResultadoProfessor, ResultadoTurma, ResultadoDiaDaSemana } from "@/lib/calculos";export async function GET(req: NextRequest) { const session = await getServerSession(); if (!session) return NextResponse.json({ error: "Não autenticado" }, { status: 401 }); const { searchParams } = new URL(req.url); const mes = parseInt(searchParams.get("mes") ?? "0"); const ano = parseInt(searchParams.get("ano") ?? "0"); if (!mes || !ano) { return NextResponse.json({ error: "Parâmetros mes e ano são obrigatórios" }, { status: 400 }); } try { // ═══════════════════════════════════════════════════════════════════════ // OTIMIZAÇÃO #1: Buscar schedule e memberships em PARALELO // ═══════════════════════════════════════════════════════════════════════ const [schedule, memberships] = await Promise.all([ getSchedule(mes, ano), getMemberMemberships(mes, ano), ]); const primeiroDia = new Date(ano, mes - 1, 1); const porProfessor: Record<string, { nomeProfessor: string; turmas: Record<string, EvoSchedule[]>; }> = {}; for (const aula of schedule) { const pid = aula.instructor || "Desconhecido"; const turmaKey = `${aula.name} - ${aula.startTime} `; if (!porProfessor[pid]) { porProfessor[pid] = { nomeProfessor: pid, turmas: {} }; } if (!porProfessor[pid].turmas[turmaKey]) { porProfessor[pid].turmas[turmaKey] = []; } porProfessor[pid].turmas[turmaKey].push(aula); } console.log("Professores encontrados (keys):", Object.keys(porProfessor)); console.log("Nomes dos professores:", Object.values(porProfessor).map(p => p.nomeProfessor)); const globalTurmaAulas = new Map<string, number>(); for (const aula of schedule) { const turmaKey = `${aula.name} - ${aula.startTime} `; globalTurmaAulas.set(turmaKey, (globalTurmaAulas.get(turmaKey) || 0) + 1); } // ═══════════════════════════════════════════════════════════════════════ // OTIMIZAÇÃO #2: Buscar TODOS os enrollments em PARALELO (batch) // ═══════════════════════════════════════════════════════════════════════ const allSessionIds = schedule.map((a) => (a as any).idAtividadeSessao as number); const uniqueSessionIds = [...new Set(allSessionIds)]; const enrollmentMap = new Map<number, EvoEnrollment[]>(); const enrollmentResults = await Promise.all( uniqueSessionIds.map(async (idSession) => { const enrollments = await getTurmaEnrollments(idSession); return { idSession, enrollments }; }) ); for (const { idSession, enrollments } of enrollmentResults) { enrollmentMap.set(idSession, enrollments); } const todosIdMatriculadas = new Set<number>(); for (const enrollments of enrollmentMap.values()) { enrollments.forEach(e => todosIdMatriculadas.add(e.idMember)); } // ═══════════════════════════════════════════════════════════════════════ // OTIMIZAÇÃO #3: Buscar grades fixas em PARALELO // ═══════════════════════════════════════════════════════════════════════ const matriculasFixasGlobal = new Map<number, EvoFixedSchedule[]>(); await Promise.all( Array.from(todosIdMatriculadas).map(async (idMem) => { const fixedScheduleArray = await getMemberFixedSchedules(idMem); matriculasFixasGlobal.set(idMem, fixedScheduleArray); }) ); // ═══════════════════════════════════════════════════════════════════════ // OTIMIZAÇÃO #4: Pré-carregar TODOS os registros de professores do Prisma // ═══════════════════════════════════════════════════════════════════════ const todosPercentuais = await prisma.professorPercentual.findMany({ orderBy: { dataInicio: "desc" }, }); const percMap = new Map<string, typeof todosPercentuais[0]>(); for (const p of todosPercentuais) { if (!percMap.has(p.idProfessorEvo)) { percMap.set(p.idProfessorEvo, p); } } // ═══════════════════════════════════════════════════════════════════════ // OTIMIZAÇÃO #5: Cache de fallback de memberships por idMember // ═══════════════════════════════════════════════════════════════════════ const membershipFallbackCache = new Map<number, EvoMemberMembership[] | null>(); async function getMemberMembershipsCached(idMember: number): Promise<EvoMemberMembership[]> { if (membershipFallbackCache.has(idMember)) { return membershipFallbackCache.get(idMember) || []; } try { const fetched = await getMemberMembershipsById(idMember); membershipFallbackCache.set(idMember, fetched); if (fetched && fetched.length > 0) { memberships.push(...fetched); } return fetched || []; } catch (e) { console.error(`Erro ao resgatar contrato idMember=${idMember}`, e); membershipFallbackCache.set(idMember, null); return []; } } const resultado: ResultadoProfessor[] = []; const professoresNovos: string[] = []; for (const pid of Object.keys(porProfessor)) { if (!percMap.has(pid)) { professoresNovos.push(pid); } } if (professoresNovos.length > 0) { await Promise.all( professoresNovos.map((pid) => prisma.professorPercentual.create({ data: { idProfessorEvo: pid, nomeProfessor: porProfessor[pid].nomeProfessor, percentual: 20, dataInicio: primeiroDia, }, }) ) ); } for (const [pid, prof] of Object.entries(porProfessor)) { const percRecord = percMap.get(pid) ?? null; const percentual = percRecord?.percentual ?? 20; const turmasCalculadas: ResultadoTurma[] = []; for (const [nomeTurma, aulas] of Object.entries(prof.turmas)) { const diasSet = new Set<number>(); for (const a of aulas) { const d = new Date(a.activityDate).getDay(); diasSet.add(d); } const diasArray = Array.from(diasSet).sort(); const nomesDias = diasArray.map((d) => DIAS_SEMANA[d]); const totalAulasGeral = aulas.length; const isCircuitoClass = nomeTurma.toLowerCase().includes("circuito"); const diasCalculados: ResultadoDiaDaSemana[] = []; if (totalAulasGeral === 0) continue; const aulasOrdenadas = [...aulas].sort( (a, b) => new Date(a.activityDate).getTime() - new Date(b.activityDate).getTime() ); // ═══════════════════════════════════════════════════════════ // OTIMIZAÇÃO #6: Usar enrollmentMap pré-carregado // ═══════════════════════════════════════════════════════════ const aulasComMatriculas = aulasOrdenadas.map((aula) => { const idSession = (aula as any).idAtividadeSessao; const enrollments = enrollmentMap.get(idSession) || []; return { aula, enrollments }; }); for (const { aula, enrollments } of aulasComMatriculas) { const dataAula = new Date(aula.activityDate); const nomeDiaStr = DIAS_SEMANA[dataAula.getDay()]; const pDia = dataAula.getDate().toString().padStart(2, "0"); const pMes = (dataAula.getMonth() + 1).toString().padStart(2, "0"); const diaDesc = `${nomeDiaStr} — ${pDia}/${pMes} às ${aula.startTime}`; const statusMatriculadas = new Map<number, { name: string, replacement: boolean; status: number }>(); enrollments.forEach(e => statusMatriculadas.set(e.idMember, { name: e.name, replacement: e.replacement, status: e.status })); const alunasDoMes: AlunaCalculo[] = []; const calcDate = new Date(ano, mes - 1, dataAula.getDate()); for (const [idMember, evoData] of Array.from(statusMatriculadas.entries())) { const isEvoReplacement = evoData.replacement; const isPresent = evoData.status === 0; let memberContracts = memberships.filter((m) => m.idMember === idMember); let precisaBuscarIndividual = memberContracts.length === 0; if (!precisaBuscarIndividual && !isCircuitoClass) { const soTemCircuito = memberContracts.every(m => (m.nameMembership || "").toLowerCase().includes("circuito")); if (soTemCircuito) { precisaBuscarIndividual = true; } } // OTIMIZAÇÃO #5 aplicada: usa cache de fallback if (precisaBuscarIndividual) { const fetchedContracts = await getMemberMembershipsCached(idMember); if (fetchedContracts.length > 0) { memberContracts = fetchedContracts; } } if (memberContracts.length === 0) { if (isPresent) { memberContracts = [{ idMember: idMember, name: evoData.name || "Aluna Avulsa/VIP", nameMembership: "Plano Avulso/VIP", membershipStatus: "Ativo", saleValue: 0, receivables: [], membershipStart: "2000-01-01T00:00:00", membershipEnd: "2099-01-01T00:00:00" } as unknown as EvoMemberMembership]; } else { continue; } } const ALUNAS_VIP_ZERO = ["juliana quintiliano", "paula vanessa carmo"]; const nomeAluna = (memberContracts[0]?.name || "").toLowerCase(); const isVipZero = ALUNAS_VIP_ZERO.some(exc => nomeAluna.includes(exc)); if (!isCircuitoClass) { const semCircuito = memberContracts.filter( (m) => !(m.nameMembership || "").toLowerCase().includes("circuito") ); if (semCircuito.length > 0) memberContracts = semCircuito; } memberContracts.sort((a, b) => { const aName = (a.nameMembership || "").toLowerCase(); const bName = (b.nameMembership || "").toLowerCase(); const className = nomeTurma.toLowerCase(); let aScore = 0; let bScore = 0; if (className.includes("slimfit") && aName.includes("slimfit")) aScore += 10; if (className.includes("slimfit") && bName.includes("slimfit")) bScore += 10; if (className.includes("circuito") && aName.includes("circuito")) aScore += 10; if (className.includes("circuito") && bName.includes("circuito")) bScore += 10; if (tipoDePlano(a.nameMembership) === "fixo") aScore += 5; if (tipoDePlano(b.nameMembership) === "fixo") bScore += 5; if (statusContrato(a) === "Ativo") aScore += 2; if (statusContrato(b) === "Ativo") bScore += 2; const aStart = a.membershipStart ? new Date(a.membershipStart) : new Date(0); const aEnd = a.membershipEnd ? new Date(a.membershipEnd) : new Date(0); const bStart = b.membershipStart ? new Date(b.membershipStart) : new Date(0); const bEnd = b.membershipEnd ? new Date(b.membershipEnd) : new Date(0); const isVigenteA = (aStart <= calcDate && aEnd >= calcDate) ? 1 : 0; const isVigenteB = (bStart <= calcDate && bEnd >= calcDate) ? 1 : 0; aScore += isVigenteA * 20; bScore += isVigenteB * 20; if (aScore === bScore) return bEnd.getTime() - aEnd.getTime(); return bScore - aScore; }); const m = memberContracts[0]; let valorMes = m.saleValue; if (m.membershipStart && m.membershipEnd) { const start = new Date(m.membershipStart); const end = new Date(m.membershipEnd); let months = (end.getFullYear() - start.getFullYear()) * 12 + (end.getMonth() - start.getMonth()); if (months <= 0) months = 1; if (months > 1 && months <= 24) valorMes = m.saleValue / months; } else { const r = m.receivables.find((rec) => !rec.canceled && rec.totalInstallments > 0); if (r && r.totalInstallments > 1 && r.totalInstallments <= 12) { valorMes = m.saleValue / r.totalInstallments; } } const tipo = tipoDePlano(m.nameMembership); if (tipo === "fixo") { valorMes = round2(valorMes * 0.97); } let isFixoEmReposicao = false; if (tipo === "fixo") { const diaAulaNum = dataAula.getDay(); const startTimeMask = aula.startTime.substring(0, 5); const dataAulaTs = dataAula.getTime(); const agendaAluna = matriculasFixasGlobal.get(m.idMember) || []; const dayOnly = (dateStr: string) => { const [y, mo, d] = dateStr.substring(0, 10).split('-').map(Number); return new Date(y, mo - 1, d).getTime(); }; const classDay = new Date(dataAula.getFullYear(), dataAula.getMonth(), dataAula.getDate()).getTime(); const ehOficialmenteDela = agendaAluna.some(ag => { if (ag.weekDay !== diaAulaNum) return false; if (!ag.startTime.startsWith(startTimeMask)) return false; const inicio = dayOnly(ag.startDate); const fim = ag.endDate ? dayOnly(ag.endDate) : Infinity; return classDay >= inicio && classDay <= fim; }); if (!ehOficialmenteDela) { isFixoEmReposicao = true; } else if (isEvoReplacement === true) { isFixoEmReposicao = true; } } let contrib = 0; if (tipo === "fixo") { let diasDeTreinoNoMes = globalTurmaAulas.get(nomeTurma) || totalAulasGeral; const diasNoMes = new Date(ano, mes, 0).getDate(); const nameStr = (m.nameMembership || "").toUpperCase(); let freq = 0; const matchAulaPre = nameStr.match(/(\d+)\s*X\s*(?:SLIM|CIRC|AULA)/); const matchAulaPos = nameStr.match(/(?:SLIM|CIRC|AULA)\s*(\d+)\s*X/); if (matchAulaPre) { freq = parseInt(matchAulaPre[1]); } else if (matchAulaPos) { freq = parseInt(matchAulaPos[1]); } else { const stringXMatches = nameStr.match(/(\d+)\s*X/g); if (stringXMatches && stringXMatches.length > 0) { for (const x of stringXMatches) freq += parseInt(x.replace(/\D/g, "")); } } if (freq > 0) diasDeTreinoNoMes = Math.round(freq * (diasNoMes / 7)); contrib = isFixoEmReposicao ? 0 : contribuicaoFixa(valorMes, percentual, diasDeTreinoNoMes); } else { if (isVipZero) { contrib = 0; } else if (isPresent) { contrib = CONTRIBUICAO_FREE; } else { continue; } } if (isFixoEmReposicao) continue; const finalStatus = statusContrato(m); alunasDoMes.push({ idMember: m.idMember, nome: m.name, tipo, statusContrato: finalStatus as any, nomeContrato: m.nameMembership || "Sem Nome", mensalidade: valorMes, pagouNoMes: temPagamentoNoMes(m, mes, ano), contribuicaoPorAula: contrib, }); } alunasDoMes.sort((a, b) => a.nome.localeCompare(b.nome)); const alunasDiaList = alunasDoMes.slice(0, 9); const isSabado = dataAula.getDay() === 6; const diaCalc = calcularDiaDaSemana({ diaDaSemana: diaDesc, totalAulasNoDia: 1, alunasCalculadas: alunasDiaList, piso: (percRecord as any)?.piso ?? 55, teto: (percRecord as any)?.teto ?? 90, }); if (isSabado) { diaCalc.valorFinalPorAula = 70.0; diaCalc.totalDiaNoMes = 70.0; diaCalc.pisoAplicado = false; diaCalc.tetoAplicado = false; diaCalc.diaDaSemana += " (Diária Fixa Sábado)"; } else { diaCalc.totalBrutoPorAula = round2(diaCalc.totalBrutoPorAula); diaCalc.valorFinalPorAula = round2(diaCalc.valorFinalPorAula); diaCalc.totalDiaNoMes = round2(diaCalc.totalDiaNoMes); } diaCalc.alunas = diaCalc.alunas.map((a) => ({ ...a, contribuicaoPorAula: round2(a.contribuicaoPorAula), })); diasCalculados.push(diaCalc); } const totalTurmaNoMes = round2( diasCalculados.reduce((sum, d) => sum + d.totalDiaNoMes, 0) ); turmasCalculadas.push({ nomeAtividade: nomeTurma, diasDaSemana: nomesDias, totalAulas: totalAulasGeral, dias: diasCalculados, totalTurmaNoMes, }); } const sabadosTrabalhados = new Set<string>(); let deducoesPorDiariaExtraDeSabado = 0; for (const t of turmasCalculadas) { for (const d of t.dias) { if (d.diaDaSemana.includes("Sábado")) { const match = d.diaDaSemana.match(/(\d{2}\/\d{2})/); if (match) { const dataKey = match[1]; if (sabadosTrabalhados.has(dataKey)) { deducoesPorDiariaExtraDeSabado += d.totalDiaNoMes; d.valorFinalPorAula = 0; d.totalDiaNoMes = 0; d.diaDaSemana = `${d.diaDaSemana.replace(' (Diária Fixa Sábado)', '')} (Diária contada em outra turma)`; } else { sabadosTrabalhados.add(dataKey); } } } } t.totalTurmaNoMes = round2( t.dias.reduce((sum, dia) => sum + dia.totalDiaNoMes, 0) ); } const turmasSabado = turmasCalculadas.filter(t => t.dias.length > 0 && t.dias.every(d => d.diaDaSemana.includes("Sábado")) ); const turmasNaoSabado = turmasCalculadas.filter(t => !t.diasDaSemana.every(d => d.includes("Sábado")) ); if (turmasSabado.length > 1) { const diasSabadoPagos: ResultadoDiaDaSemana[] = []; for (const t of turmasSabado) { for (const d of t.dias) { if (d.totalDiaNoMes > 0) { diasSabadoPagos.push(d); } } } const totalSabadoNoMes = round2(diasSabadoPagos.reduce((s, d) => s + d.totalDiaNoMes, 0)); const turmaSabadoMerged: ResultadoTurma = { nomeAtividade: "SLIMFIT B / Circuito B - 09:00", diasDaSemana: ["Sábado"], totalAulas: diasSabadoPagos.length, dias: diasSabadoPagos, totalTurmaNoMes: totalSabadoNoMes, }; turmasCalculadas.splice(0, turmasCalculadas.length, ...turmasNaoSabado, turmaSabadoMerged); } const totalGeralNoMes = round2( turmasCalculadas.reduce((sum, t) => sum + t.totalTurmaNoMes, 0) ); resultado.push({ idProfessorEvo: pid, nomeProfessor: prof.nomeProfessor, percentual, turmas: turmasCalculadas, totalGeralNoMes, }); } resultado.sort((a, b) => a.nomeProfessor.localeCompare(b.nomeProfessor)); return NextResponse.json({ mes, ano, professores: resultado }); } catch (err: unknown) { const msg = err instanceof Error ? err.message : "Erro desconhecido"; return NextResponse.json({ error: msg }, { status: 500 }); }}
