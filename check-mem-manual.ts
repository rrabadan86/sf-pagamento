import { config } from 'dotenv'; config({ path: '.env' }); import fetch from 'node-fetch'; async function run() { const url = process.env.EVO_BASE_URL + '/api/v3/membermembership?statusMemberMembership=1&take=50&skip=0'; const creds = Buffer.from(process.env.EVO_DNS + ':' + process.env.EVO_TOKEN).toString('base64'); const res = await fetch(url, { headers: { Authorization: 'Basic ' + creds } }); const json = await res.json(); console.log('Len:', json.length); } run();  
