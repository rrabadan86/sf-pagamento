import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function run() { const mes = 2; const ano = 2026; const dataInicioBusca = new Date(ano, mes - 1, 1); const dataFimBusca = new Date(ano, mes, 0, 23, 59, 59); const contratos = await prisma.contrato.findMany({ where: { OR: [ { dataInicio: { lte: dataFimBusca }, dataFim: { gte: dataInicioBusca } } ] }, include: { aluno: true } }); console.log('Contratos Ativos:', contratos.length); } run();  
